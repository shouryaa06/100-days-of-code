#include <stdio.h>

int main()
{
    int a,b,c,d;
    int sum1 ,sum2;
    int L;
    printf("enter the limit");
    scanf("%d",&L);
    for(a=1;a<=L;a++)
    {
        for (b=a;b<=L;b++)
    {
        sum1=a*a*a +b*b*b;
        for(c=a+1;c<=L;c++)
        {
            for(d=c;d<=L;d++)
            {
                sum2=c*c*c+d*d*d;
                if(sum1==sum2)
                {printf("%d=%d^3+%d^3=%d^3+%d^3\n");
                sum1,a,b,c,d;
                }
            }
        }
    }
    }
    return 0;
}
